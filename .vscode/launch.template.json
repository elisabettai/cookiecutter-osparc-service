{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Run Test",
      "type": "python",
      "request": "launch",
      "module": "pytest",
      "args": [
        "--ff",
        "--log-cli-level=INFO",
        "--pdb",
        "--setup-show",
        "-sx",
        "-vv",
        "${file}"
      ],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "justMyCode": false
    }
  ]
}
